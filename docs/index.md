# factors-no-more

Guardrails and minimal estimators that operationalize **'ditch the factors associated with'**.

Install:

```bash
poetry install
```

Build docs locally:

```bash
poetry run mkdocs serve
```
